{
  "name": "court-subgraph",
  "version": "0.0.1",
  "scripts": {
    "build": "graph build",
    "codegen": "rm -rf types && graph codegen -o ./types",
    "deploy:rpc": "NETWORK=rpc ./scripts/deploy.sh",
    "draw-schema": "./scripts/draw-schema.sh"
  },
  "devDependencies": {
    "graphqlviz": "^3.1.0"
  },
  "dependencies": {
    "@aragon/court": "1.1.2",
    "@aragon/minime": "1.0.0",
    "@graphprotocol/graph-cli": "^0.16.0",
    "@graphprotocol/graph-ts": "^0.16.0"
  }
}
